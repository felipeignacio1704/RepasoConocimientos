var frase = "HolaMundo";
var verLargo = frase.length; // Devuelve el largo de la cadena
console.log("Largo de la frase:", verLargo);
var buscarPalabra = frase.indexOf("Mundo"); // Devuelve la posición de la palabra
console.log("Posición de 'Mundo':", buscarPalabra);
var buscarPalabra2 = frase.lastIndexOf("Mundo", 30); // Devuelve la última posición de la palabra dentro de los primeros 30 caracteres
console.log("Última posición de 'Mundo':", buscarPalabra2);

var frase2 = "Departamento de Sistemas";
varBusqueda = search("Sistemas"); // Devuelve la posición de la palabra
varbusqueda2 = string.slice(0, 11); // Devuelve una parte de la cadena desde el índice 0 hasta el índice 11
//substring
varbusqueda3 = string.substring(0, 11); // Devuelve una parte de la cadena desde el índice 0 hasta el índice 11
//substr
varbusqueda4 = string.substr(0, 11); // Devuelve una parte de la cadena desde el índice 0 con una longitud de 11 caracteres
//replace
varbusqueda5 = string.replace("Sistemas", "Informática"); // Reemplaza "Sistemas" por "Informática"
//toUpperCase
varbusqueda6 = string.toUpperCase(); // Convierte toda la cadena a mayúsculas
//toLowerCase
varbusqueda7 = string.toLowerCase(); // Convierte toda la cadena a minúsculas
//concat
varbusqueda8 = string.concat(" y Tecnología"); // Concatena " y Tecnología"
//trim
varbusqueda9 = string.trim(); // Elimina los espacios en blanco al inicio y al final de la cadena
//split
varbusqueda10 = string.split(" "); // Divide la cadena en un array usando el espacio como separador
//charAt
varbusqueda11 = string.charAt(0); // Devuelve el carácter en la posición 0
//charCodeAt
varbusqueda12 = string.charCodeAt(0); // Devuelve el código ASCII del carácter en la posición 0